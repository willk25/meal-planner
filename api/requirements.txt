# Minimal requirements for Vercel serverless functions only
# This keeps the bundle size under 250 MB

# Database
supabase>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Lightweight recipe scraping (replaces heavy recipe-scrapers)
beautifulsoup4>=4.14.0
requests>=2.31.0
